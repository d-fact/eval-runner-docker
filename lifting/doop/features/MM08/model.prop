(!NewCompound1 \/ MobilMedia) /\ (!MobilMedia \/ NewCompound1) /\ (!Media \/ Video \/ _includeMMAPI \/ Photo) /\ (!Video \/ Media) /\ (!_includeMMAPI \/ Media) /\ (!Photo \/ Media) /\ (!Derivatives \/ _Derivatives) /\ (!_Derivatives \/ Derivatives) /\ (!Video \/ includeVideo) /\ (!includeVideo \/ Video) /\ (!captureVideo \/ Video) /\ (!simulatePlayVideo \/ Video) /\ (!_includeMMAPI \/ includeMMAPI) /\ (!includeMMAPI \/ _includeMMAPI) /\ (!Photo \/ includePhotoAlbum) /\ (!includePhotoAlbum \/ Photo) /\ (!includeSmsFeature \/ Photo) /\ (!capturePhoto \/ Photo) /\ (!MobilMedia \/ Media) /\ (!Media \/ MobilMedia) /\ (!includeSorting \/ MobilMedia) /\ (!includeFavourites \/ MobilMedia) /\ (!includeCopyPhoto \/ MobilMedia) /\ (!MobilMedia \/ Derivatives) /\ (!Derivatives \/ MobilMedia) /\ (!includePrivacy \/ MobilMedia) /\ (!x_CopyPhotoOrSMS \/ _Derivatives) /\ (!x_PhotoAlbumOrMusic \/ _Derivatives) /\ (!x_NotPhotoAlbum \/ _Derivatives) /\ (!x_notMisc \/ _Derivatives) /\ (!x_MusicAndVideo \/ _Derivatives) /\ (!x_NotMusic \/ _Derivatives) /\ (!x_PhotoAlbumAndVideoOrMusic \/ _Derivatives) /\ (!x_CapturePhotoOrVideo \/ _Derivatives) /\ (!x_NotPrivacy \/ _Derivatives) /\ (!x_SMSOrCapturePhoto \/ _Derivatives) /\ (!x_misc \/ _Derivatives) /\ (!x_MusicOrVideo \/ _Derivatives) /\ (!x_SMSOrCapturePhotoOrCaptureVideo \/ _Derivatives) /\ (!x_PhotoAlbumOrMusicOrVideo \/ _Derivatives) /\ (!x_CopyPhotoOrSMSOrCapturePhoto \/ _Derivatives) /\ (!x_notSimulatePlayVideo \/ _Derivatives) /\ (!x_CopyPhotoOrSMS \/ includeCopyPhoto \/ includeSmsFeature) /\ (!includeCopyPhoto \/ x_CopyPhotoOrSMS) /\ (!includeSmsFeature \/ x_CopyPhotoOrSMS) /\ (!x_PhotoAlbumOrMusic \/ includePhotoAlbum \/ includeMMAPI) /\ (!includePhotoAlbum \/ x_PhotoAlbumOrMusic) /\ (!includeMMAPI \/ x_PhotoAlbumOrMusic) /\ (!x_NotPhotoAlbum \/ !includePhotoAlbum) /\ (includePhotoAlbum \/ x_NotPhotoAlbum) /\ (!x_NotMusic \/ !includeMMAPI) /\ (includeMMAPI \/ x_NotMusic) /\ (!x_misc \/ includeMMAPI \/ includeMMAPI \/ includeVideo) /\ (!x_misc \/ includeMMAPI \/ includeMMAPI \/ includePhotoAlbum) /\ (!x_misc \/ includeMMAPI \/ includeVideo \/ includeVideo) /\ (!x_misc \/ includeMMAPI \/ includeVideo \/ includePhotoAlbum) /\ (!x_misc \/ includePhotoAlbum \/ includeMMAPI \/ includeVideo) /\ (!x_misc \/ includePhotoAlbum \/ includeMMAPI \/ includePhotoAlbum) /\ (!x_misc \/ includePhotoAlbum \/ includeVideo \/ includeVideo) /\ (!x_misc \/ includePhotoAlbum \/ includeVideo \/ includePhotoAlbum) /\ (!includeMMAPI \/ !includePhotoAlbum \/ x_misc) /\ (!includeMMAPI \/ !includeVideo \/ x_misc) /\ (!includeVideo \/ !includePhotoAlbum \/ x_misc) /\ (!x_notMisc \/ !x_misc) /\ (x_misc \/ x_notMisc) /\ (!x_PhotoAlbumAndVideoOrMusic \/ includeMMAPI \/ includePhotoAlbum) /\ (!x_PhotoAlbumAndVideoOrMusic \/ includeMMAPI \/ includeVideo) /\ (!x_PhotoAlbumAndVideoOrMusic \/ includePhotoAlbum \/ includePhotoAlbum) /\ (!x_PhotoAlbumAndVideoOrMusic \/ includePhotoAlbum \/ includeVideo) /\ (!includeMMAPI \/ !includePhotoAlbum \/ x_PhotoAlbumAndVideoOrMusic) /\ (!includePhotoAlbum \/ !includeVideo \/ x_PhotoAlbumAndVideoOrMusic) /\ (!x_CapturePhotoOrVideo \/ capturePhoto \/ captureVideo) /\ (!capturePhoto \/ x_CapturePhotoOrVideo) /\ (!captureVideo \/ x_CapturePhotoOrVideo) /\ (!x_NotPrivacy \/ !includePrivacy) /\ (includePrivacy \/ x_NotPrivacy) /\ (!x_MusicOrVideo \/ includeMMAPI \/ includeVideo) /\ (!includeMMAPI \/ x_MusicOrVideo) /\ (!includeVideo \/ x_MusicOrVideo) /\ (!x_SMSOrCapturePhotoOrCaptureVideo \/ includeSmsFeature \/ capturePhoto \/ captureVideo) /\ (!includeSmsFeature \/ x_SMSOrCapturePhotoOrCaptureVideo) /\ (!capturePhoto \/ x_SMSOrCapturePhotoOrCaptureVideo) /\ (!captureVideo \/ x_SMSOrCapturePhotoOrCaptureVideo) /\ (!x_PhotoAlbumOrMusicOrVideo \/ includePhotoAlbum \/ includeMMAPI \/ includeVideo) /\ (!includePhotoAlbum \/ x_PhotoAlbumOrMusicOrVideo) /\ (!includeMMAPI \/ x_PhotoAlbumOrMusicOrVideo) /\ (!includeVideo \/ x_PhotoAlbumOrMusicOrVideo) /\ (!x_CopyPhotoOrSMSOrCapturePhoto \/ includeCopyPhoto \/ includeSmsFeature \/ capturePhoto) /\ (!includeCopyPhoto \/ x_CopyPhotoOrSMSOrCapturePhoto) /\ (!includeSmsFeature \/ x_CopyPhotoOrSMSOrCapturePhoto) /\ (!capturePhoto \/ x_CopyPhotoOrSMSOrCapturePhoto) /\ (!x_notSimulatePlayVideo \/ !simulatePlayVideo) /\ (simulatePlayVideo \/ x_notSimulatePlayVideo) /\ (!x_MusicAndVideo \/ includeMMAPI) /\ (!x_MusicAndVideo \/ includeVideo) /\ (!includeMMAPI \/ !includeVideo \/ x_MusicAndVideo) /\ (!x_SMSOrCapturePhoto \/ includeSmsFeature \/ capturePhoto) /\ (!includeSmsFeature \/ x_SMSOrCapturePhoto) /\ (!capturePhoto \/ x_SMSOrCapturePhoto) /\ (NewCompound1)
